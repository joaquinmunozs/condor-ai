/**
 * Prueba social (Fase 11) — DESACTIVADA a propósito.
 *
 * Se eliminaron las métricas y el testimonio porque eran PLACEHOLDER inventado,
 * y la Fase 11 prohíbe prueba social falsa (daña la marca). Esta sección vuelve
 * cuando haya métricas y testimonios REALES de clientes de condor.ai.
 *
 * Para reactivarla: rellenar con datos reales y volver a montar <Prueba /> en
 * pages/Home.tsx. El CSS de soporte (.metricas / .testimonio) sigue en App.css.
 */
export default function Prueba() {
  return null;
}
